{
  "name": "@ghuser/ghuser.io",
  "version": "0.0.1",
  "description": "Better GitHub profiles.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AurelienLourot/ghuser.io.git"
  },
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "start": "reframe server",
    "local": "reframe start",
    "deploy": "reframe build && rm -rf dist/previous/ && ./up && ./up production && ./up stack status"
  },
  "dependencies": {
    "@brillout/fetch": "^0.1.0",
    "@reframe/cli": "^0.1.13",
    "@reframe/react-kit": "0.1.13",
    "react": "^16.3.2"
  }
}
